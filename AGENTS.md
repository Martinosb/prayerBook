# Expo HAS CHANGED

This project is pinned to **Expo SDK 54** (to match the Expo Go app currently on the Play Store). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code. Do not let `expo install` or `npx create-expo-app` bump this to a newer SDK — always target `~54.x` for `expo` and run `npx expo install --check` against SDK 54.
