EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# AI scripture suggestions and voice-note transcription are served by
# Supabase Edge Functions deployed alongside the project above (see
# supabase/functions/ and CLAUDE.md) — no separate API base URL needed.
